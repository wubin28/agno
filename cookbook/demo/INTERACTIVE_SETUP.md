# 🔐 交互式 API Key 输入功能

## 功能概述

为了提高安全性和易用性，`run.py` 现在支持**交互式 API Key 输入**，让你无需将敏感信息保存到文件或环境变量中。

## 主要特性

### ✅ 安全性
- **隐藏输入**：使用 Python 的 `getpass` 模块，输入时不显示或显示为星号
- **会话临时**：API key 仅在当前运行会话中有效
- **不写入文件**：不会保存到任何配置文件
- **内存清除**：程序关闭后自动清除

### ✅ 易用性
- **自动检测**：程序启动时自动检查是否需要 API key
- **清晰提示**：友好的交互界面，提供获取 API key 的链接
- **可选输入**：对于可选的 API key（如 OpenAI），可以选择跳过
- **智能跳过**：如果环境变量已设置，自动使用，不会重复询问

### ✅ 灵活性
- **三种方式**：支持交互式输入、环境变量、配置脚本
- **优先级明确**：环境变量 > 交互式输入
- **可中断**：任何时候按 Ctrl+C 可以安全退出

## 工作流程

```
启动 run.py
    ↓
检查 DEEPSEEK_API_KEY
    ↓
┌─────────────────┐
│ 环境变量已设置？ │
└─────────────────┘
    ↓           ↓
   是          否
    ↓           ↓
  跳过     交互式输入
    ↓           ↓
    └───────────┘
         ↓
   验证 API key
         ↓
    设置到环境变量
         ↓
    导入并启动 Agents
```

## 代码实现

### 核心函数

```python
def check_api_keys():
    """Check for required API keys and prompt if missing."""
    # 检查 DeepSeek API Key
    if not os.getenv("DEEPSEEK_API_KEY"):
        print("\n⚠️  DEEPSEEK_API_KEY not found")
        api_key = getpass.getpass("DeepSeek API Key (hidden): ")
        os.environ["DEEPSEEK_API_KEY"] = api_key
    
    # 检查 OpenAI API Key (可选)
    if not os.getenv("OPENAI_API_KEY"):
        response = input("Enter OpenAI key? (y/n): ")
        if response == 'y':
            openai_key = getpass.getpass("OpenAI API Key (hidden): ")
            os.environ["OPENAI_API_KEY"] = openai_key
```

### 关键技术点

1. **getpass.getpass()**
   - Python 标准库函数
   - 隐藏用户输入
   - 跨平台支持

2. **os.environ**
   - 运行时设置环境变量
   - 只影响当前进程及子进程
   - 程序退出后自动清除

3. **执行顺序**
   - 先检查和设置 API key
   - 再导入 agents（避免导入时报错）

## 使用示例

### 场景 1: 首次运行（无环境变量）

```bash
$ python run.py

============================================================
AgentOS API Key Configuration
============================================================

⚠️  DEEPSEEK_API_KEY not found in environment
Please enter your DeepSeek API key:
(Get one at: https://platform.deepseek.com/)
DeepSeek API Key (hidden): ****************  # 你输入的内容
✓ DeepSeek API key set for this session

⚠️  OPENAI_API_KEY not found (optional)
The knowledge base uses OpenAI embeddings.
Would you like to enter it now? (y/n): n
ℹ️  Skipping OpenAI API key (some features may not work)

============================================================
Starting AgentOS...
============================================================

╔═══════════════ AgentOS ════════════════╗
║                                        ║
║          https://os.agno.com/          ║
║                                        ║
║  OS running on: http://localhost:7777  ║
║                                        ║
╚════════════════════════════════════════╝
```

### 场景 2: 环境变量已设置

```bash
$ export DEEPSEEK_API_KEY="sk-xxxxx"
$ python run.py

============================================================
AgentOS API Key Configuration
============================================================

✓ DEEPSEEK_API_KEY found
✓ OPENAI_API_KEY found

============================================================
Starting AgentOS...
============================================================
```

### 场景 3: 用户取消

```bash
$ python run.py

⚠️  DEEPSEEK_API_KEY not found in environment
Please enter your DeepSeek API key:
(Get one at: https://platform.deepseek.com/)
DeepSeek API Key (hidden): ^C

❌ Setup cancelled by user
```

## 与其他方法的比较

| 方法 | 安全性 | 便利性 | 持久性 | 推荐场景 |
|------|--------|--------|--------|----------|
| **交互式输入** | 🟢 最高 | 🟡 中等 | ❌ 无 | 临时测试、演示 |
| **环境变量** | 🟡 中等 | 🟢 最高 | ✅ 会话级 | 开发调试 |
| **配置文件** | 🔴 最低 | 🟢 最高 | ✅ 永久 | 生产环境（加密） |
| **setup脚本** | 🟡 中等 | 🟢 最高 | ✅ 永久 | 长期使用 |

## 安全最佳实践

### ✅ 推荐做法

1. **开发阶段**：使用交互式输入或会话级环境变量
2. **测试阶段**：使用环境变量文件（不提交到 Git）
3. **生产环境**：使用加密的配置管理系统（如 Vault）
4. **共享演示**：使用交互式输入，避免泄露

### ❌ 避免做法

1. ❌ 将 API key 硬编码到源代码
2. ❌ 将 API key 提交到版本控制系统
3. ❌ 在日志中打印完整的 API key
4. ❌ 在公共文档中分享真实的 API key

## 错误处理

### 空输入

```python
if not api_key.strip():
    print("❌ Error: API key cannot be empty")
    exit(1)
```

### 用户中断

```python
try:
    api_key = getpass.getpass("API Key: ")
except KeyboardInterrupt:
    print("\n❌ Setup cancelled by user")
    exit(1)
```

### API key 无效

程序会在实际调用 API 时失败，并显示相应的错误信息。

## 兼容性

### 支持的终端

- ✅ Linux/Unix 标准终端
- ✅ macOS Terminal
- ✅ Windows PowerShell
- ✅ Windows Command Prompt
- ✅ WSL2 (你的环境)
- ⚠️ 某些 IDE 集成终端（可能不隐藏输入）

### Python 版本

- 需要 Python 3.6+
- 使用标准库，无需额外依赖

## 扩展和自定义

### 添加更多 API key 检查

在 `check_api_keys()` 函数中添加：

```python
# 检查其他 API key
if not os.getenv("ANOTHER_API_KEY"):
    print("\n⚠️  ANOTHER_API_KEY not found")
    response = input("Would you like to enter it? (y/n): ")
    if response == 'y':
        key = getpass.getpass("Another API Key (hidden): ")
        os.environ["ANOTHER_API_KEY"] = key
```

### 从文件读取（如果存在）

```python
def check_api_keys():
    # 先尝试从 .env 文件读取
    env_file = Path(".env")
    if env_file.exists():
        from dotenv import load_dotenv
        load_dotenv()
    
    # 然后检查环境变量...
```

## 故障排除

### 问题：输入仍然可见

**解决方案：**
- 在真实终端中运行，而不是 IDE 的集成终端
- 或使用环境变量方式

### 问题：getpass 模块不可用

**解决方案：**
- 检查 Python 版本（需要 3.6+）
- getpass 是标准库，应该始终可用

### 问题：API key 设置后仍然报错

**检查：**
1. API key 是否正确（无多余空格）
2. API key 是否有效（登录 DeepSeek 平台验证）
3. 网络是否可以访问 api.deepseek.com

## 总结

交互式 API Key 输入功能提供了：
- 🔒 **安全性**：不保存敏感信息
- 🚀 **便利性**：即时使用，无需配置
- 🎯 **灵活性**：支持多种使用场景
- ✨ **用户体验**：友好的交互界面

这是在安全性和易用性之间的最佳平衡！

---

**相关文档：**
- `QUICKSTART_DEEPSEEK.md` - 快速启动指南
- `DEEPSEEK_SETUP.md` - 详细设置说明
- `TESTING_INSTRUCTIONS.md` - 测试指南
- `TEST_NOW.txt` - 快速测试命令

